apiVersion: v1
kind: Pod
metadata:
  labels:
    run: my-backend
  name: my-backend
spec:
  containers:
  - image: tsc-backend
    name: my-backend
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
